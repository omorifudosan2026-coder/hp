<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  {% if noindex %}<meta name="robots" content="noindex, nofollow">{% endif %}
  <title>{% if title %}{{ title }} | 大森不動産{% else %}大森不動産 | リノベーションで叶える、あなたらしい暮らし{% endif %}</title>
  {% set metaDesc = description or site.description %}
  <meta name="description" content="{{ metaDesc | escape }}">
  <link rel="canonical" href="{{ site.url }}{{ page.url }}">
  <meta property="og:type" content="website">
  <meta property="og:site_name" content="{{ site.name | escape }}">
  <meta property="og:title" content="{% if title %}{{ title }} | {{ site.name }}{% else %}{{ site.name }} | リノベーションで叶える、あなたらしい暮らし{% endif %}">
  <meta property="og:description" content="{{ metaDesc | escape }}">
  <meta property="og:url" content="{{ site.url }}{{ page.url }}">
  {% set ogImageFull = ogImage if ogImage else (site.url + '/images/hero-bg.jpg') %}
  <meta property="og:image" content="{{ ogImageFull }}">
  <meta name="twitter:card" content="summary_large_image">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Zen+Kaku+Gothic+New:wght@300;400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/main.css{% if mainCssCacheBust %}?v={{ mainCssCacheBust }}{% endif %}">
  <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
</head>
<body class="bg-white" data-page="{{ activePage }}">
<script>document.documentElement.classList.add('hero-anim');</script>

{% if firebase %}
  <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-auth-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-firestore-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-storage-compat.js"></script>
  <script src="/firebase-config.js"></script>
{% endif %}

<!-- Header -->
<header id="header" class="fixed top-0 left-0 right-0 z-50">
  <div class="max-w-7xl mx-auto px-6 md:px-10 py-1.5 md:py-2">
    <div class="flex items-center justify-between h-14 md:h-16">
      <a href="/index.html" class="header-logo-link inline-flex items-center gap-3 shrink-0 min-h-0">
        <img src="/images/logo.png" alt="" class="header-logo-img shrink-0" width="120" height="160" decoding="async">
        <span class="header-brand font-bold text-lg md:text-xl text-ink">大森<span style="color:#E8621A">不動産</span></span>
      </a>

      <!-- Desktop nav -->
      <nav class="hidden lg:flex items-center gap-6 xl:gap-8">
        <a href="/concept.html" class="header-nav-link {{ 'font-medium' if activePage == 'concept' else '' }}">選ばれる理由</a>
        <!-- サービス ドロップダウン -->
        <div x-data="{ open: false }" @mouseenter="open = true" @mouseleave="open = false" class="relative">
          <button type="button" class="header-nav-link flex items-center gap-1 {{ 'font-medium' if activePage in ['service-kaitore', 'service-renovation', 'service-income'] else '' }}" :aria-expanded="open">
            サービス
            <svg :class="open ? 'rotate-180' : ''" class="w-3 h-3 mt-0.5 transition-transform duration-150" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
          </button>
          <div x-show="open" x-transition:enter="transition ease-out duration-150" x-transition:enter-start="opacity-0 -translate-y-1" x-transition:enter-end="opacity-100 translate-y-0" x-transition:leave="transition ease-in duration-100" x-transition:leave-start="opacity-100 translate-y-0" x-transition:leave-end="opacity-0 -translate-y-1" class="absolute top-full left-1/2 -translate-x-1/2 pt-3 z-50" style="display: none;">
            <div class="bg-white border border-[#DDD9D2] rounded-sm shadow-lg py-1.5 min-w-[11rem]">
              <a href="/service-kaitore.html" class="flex items-center gap-2 px-5 py-2.5 text-sm text-ink hover:text-[#E8621A] hover:bg-cream transition-colors {{ 'font-medium text-[#E8621A]' if activePage == 'service-kaitore' else '' }}">
                <span class="text-[#E8621A] text-xs font-mono-label">01</span>買取再販
              </a>
              <a href="/service-renovation.html" class="flex items-center gap-2 px-5 py-2.5 text-sm text-ink hover:text-[#E8621A] hover:bg-cream transition-colors {{ 'font-medium text-[#E8621A]' if activePage == 'service-renovation' else '' }}">
                <span class="text-[#E8621A] text-xs font-mono-label">02</span>仲介＋リノベ
              </a>
              <a href="/service-income.html" class="flex items-center gap-2 px-5 py-2.5 text-sm text-ink hover:text-[#E8621A] hover:bg-cream transition-colors {{ 'font-medium text-[#E8621A]' if activePage == 'service-income' else '' }}">
                <span class="text-[#E8621A] text-xs font-mono-label">03</span>収益不動産
              </a>
            </div>
          </div>
        </div>
        <a href="/works.html" class="header-nav-link {{ 'font-medium' if activePage == 'works' else '' }}">施工事例</a>
        <a href="/story.html" class="header-nav-link {{ 'font-medium' if activePage == 'story' else '' }}">ストーリー</a>
        <a href="/news.html" class="header-nav-link {{ 'font-medium' if activePage == 'news' else '' }}">ブログ</a>
        <a href="/contact.html" class="site-btn site-btn--sm site-btn--outline-ink"><span class="site-btn__inner">無料相談を予約する</span></a>
      </nav>

      <!-- Mobile button -->
      <button type="button" id="mobile-menu-btn" class="lg:hidden p-2 hamburger-btn" aria-controls="mobile-menu" aria-expanded="false" aria-label="メニューを開く">
        <span class="hamburger-btn__lines" aria-hidden="true">
          <span class="hamburger-btn__line"></span>
          <span class="hamburger-btn__line"></span>
          <span class="hamburger-btn__line"></span>
        </span>
      </button>
    </div>
  </div>

  <!-- Mobile menu -->
  <div id="mobile-menu" class="mobile-menu-panel lg:hidden mobile-menu-panel--collapsed overflow-hidden" role="navigation" aria-label="モバイルメニュー">
    <div class="bg-cream border-t border-[#DDD9D2] px-6 py-4">
      <nav class="flex flex-col gap-0 divide-y divide-[#DDD9D2]">
        <a href="/concept.html" class="py-3.5 text-lg text-ink hover:text-[#E8621A] transition-colors">選ばれる理由</a>
        <!-- サービス アコーディオン -->
        <div x-data="{ open: false }">
          <button type="button" @click="open = !open" class="w-full flex items-center justify-between py-3.5 text-lg text-ink hover:text-[#E8621A] transition-colors" :aria-expanded="open">
            サービス
            <svg :class="open ? 'rotate-180' : ''" class="w-4 h-4 transition-transform duration-150 text-muted" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
          </button>
          <div x-show="open" x-transition:enter="transition ease-out duration-150" x-transition:enter-start="opacity-0 -translate-y-1" x-transition:enter-end="opacity-100 translate-y-0" x-transition:leave="transition ease-in duration-100" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0" class="pb-2 pl-3 flex flex-col gap-0 border-l-2 border-[#E8621A]/30 ml-1" style="display: none;">
            <a href="/service-kaitore.html" class="py-2.5 text-base text-ink hover:text-[#E8621A] transition-colors">買取再販</a>
            <a href="/service-renovation.html" class="py-2.5 text-base text-ink hover:text-[#E8621A] transition-colors">仲介＋リノベ</a>
            <a href="/service-income.html" class="py-2.5 text-base text-ink hover:text-[#E8621A] transition-colors">収益不動産</a>
          </div>
        </div>
        <a href="/works.html" class="py-3.5 text-lg text-ink hover:text-[#E8621A] transition-colors">施工事例</a>
        <a href="/story.html" class="py-3.5 text-lg text-ink hover:text-[#E8621A] transition-colors">ストーリー</a>
        <a href="/news.html" class="py-3.5 text-lg text-ink hover:text-[#E8621A] transition-colors">ブログ</a>
        <a href="/contact.html" class="pt-4 pb-2">
          <span class="site-btn site-btn--menu site-btn--outline-ink inline-flex"><span class="site-btn__inner">無料相談を予約する</span></span>
        </a>
      </nav>
    </div>
  </div>
</header>

<!-- Page Content -->
{{ content | safe }}

<!-- Footer（CTA のクリーム地と区別するためネイビー帯） -->
<footer class="bg-[#18263E] text-white pt-16 pb-10">
  <div class="max-w-7xl mx-auto px-6 md:px-10">
    <div class="grid md:grid-cols-4 gap-12 pb-12 border-b border-white/15">
      <div class="md:col-span-1">
        <div class="flex items-center gap-3 mb-5">
          <span class="inline-flex items-center justify-center p-1.5 rounded-xl bg-white ring-1 ring-black/10 shadow-sm shadow-black/20">
            <img src="/images/logo.png" alt="大森不動産" class="h-8 w-auto">
          </span>
          <span class="header-brand font-bold text-lg text-white">大森<span style="color:#E8621A">不動産</span></span>
        </div>
        <p class="text-white/80 text-xs leading-relaxed">工務店がいる不動産会社。<br>物件探しからリフォームまで、価値まで見て提案します。</p>
      </div>
      <div>
        <p class="font-mono-label text-[0.65rem] tracking-widest text-white/60 uppercase mb-5">Service</p>
        <ul class="space-y-3 text-sm">
          <li><a href="/service-kaitore.html" class="text-white/85 hover:text-white transition-colors">買取再販</a></li>
          <li><a href="/service-renovation.html" class="text-white/85 hover:text-white transition-colors">仲介＋リノベ</a></li>
          <li><a href="/service-income.html" class="text-white/85 hover:text-white transition-colors">収益不動産</a></li>
        </ul>
      </div>
      <div>
        <p class="font-mono-label text-[0.65rem] tracking-widest text-white/60 uppercase mb-5">Info</p>
        <ul class="space-y-3 text-sm">
          <li><a href="/concept.html" class="text-white/85 hover:text-white transition-colors">選ばれる理由</a></li>
          <li><a href="/works.html" class="text-white/85 hover:text-white transition-colors">施工事例</a></li>
          <li><a href="/story.html" class="text-white/85 hover:text-white transition-colors">ストーリー</a></li>
          <li><a href="/news.html" class="text-white/85 hover:text-white transition-colors">ブログ</a></li>
          <li><a href="/company.html" class="text-white/85 hover:text-white transition-colors">会社概要</a></li>
        </ul>
      </div>
      <div>
        <p class="font-mono-label text-[0.65rem] tracking-widest text-white/60 uppercase mb-5">Contact</p>
        <a href="/contact.html" class="site-btn site-btn--footer site-btn--outline-light mb-5 inline-flex"><span class="site-btn__inner">無料相談を予約する →</span></a>
        <p class="text-white font-bold text-lg mb-1">0120-000-0000</p>
        <p class="text-white/75 text-xs">9:00-18:00 / 定休日：水曜日</p>
      </div>
    </div>
    <div class="flex flex-col md:flex-row justify-between items-center gap-4 pt-8">
      <p class="text-xs text-white/60">&copy; 2026 大森不動産. All rights reserved.</p>
      <div class="flex gap-6 text-xs text-white/60">
        <a href="/privacy.html" class="hover:text-white transition-colors">プライバシーポリシー</a>
        <a href="/terms.html" class="hover:text-white transition-colors">利用規約</a>
        <a href="/sitemap.html" class="hover:text-white transition-colors">サイトマップ</a>
      </div>
    </div>
  </div>
</footer>

<!-- Scripts -->
<script src="/js/display-utils.js"></script>
<script src="/js/script.js"></script>
<script src="/js/header.js"></script>
{% if pageScript %}<script src="/{{ pageScript }}"></script>{% endif %}

<!-- Intersection Observer -->
<script>
(function(){
  var els = document.querySelectorAll('[data-reveal]');
  if (!els.length) return;
  var io = new IntersectionObserver(function(entries){
    entries.forEach(function(e){
      if(e.isIntersecting){ e.target.classList.add('is-revealed'); io.unobserve(e.target); }
    });
  }, { threshold: 0.08, rootMargin: '0px 0px -40px 0px' });
  els.forEach(function(el){ io.observe(el); });
})();
(function(){
  var hero = document.querySelector('.hero-intro');
  if (!hero) return;
  function activate(){
    hero.classList.add('hero-intro--active');
  }
  var reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  var heading = document.getElementById('hero-heading');
  if (!reduced && heading) {
    var counter = 0;
    function wrapTextNode(textNode) {
      var text = textNode.textContent;
      var parent = textNode.parentNode;
      if (!parent || !text) return;
      if (!/[^\s]/.test(text)) {
        textNode.textContent = '';
        return;
      }
      var frag = document.createDocumentFragment();
      Array.prototype.forEach.call(Array.from(text), function(ch) {
        if (ch === '\n' || ch === '\r') return;
        var span = document.createElement('span');
        span.className = 'hero-char';
        span.style.setProperty('--ci', String(counter));
        span.textContent = ch;
        frag.appendChild(span);
        counter += 1;
      });
      if (!frag.childNodes.length) {
        parent.removeChild(textNode);
        return;
      }
      parent.replaceChild(frag, textNode);
    }
    function walk(el) {
      var list = Array.prototype.slice.call(el.childNodes);
      list.forEach(function(child) {
        if (child.nodeType === 3) wrapTextNode(child);
        else if (child.nodeType === 1 && child.tagName !== 'BR') walk(child);
      });
    }
    walk(heading);
    if (counter > 0) hero.classList.add('hero-intro--chars-prepared');
  }
  if (reduced) {
    activate();
    return;
  }
  requestAnimationFrame(function(){
    requestAnimationFrame(function(){
      activate();
    });
  });
})();
(function(){
  function setHeroCountFinals(root) {
    var scope = root || document;
    scope.querySelectorAll('[data-hero-count]').forEach(function(el) {
      var v = el.getAttribute('data-hero-count');
      if (v != null) el.textContent = v;
    });
  }
  function runHeroCountUp(root) {
    var scope = root || document;
    scope.querySelectorAll('[data-hero-count]').forEach(function(el) {
      var target = parseInt(el.getAttribute('data-hero-count'), 10);
      if (isNaN(target)) return;
      var start = performance.now();
      var dur = 1100;
      function easeOutCubic(t) {
        return 1 - Math.pow(1 - t, 3);
      }
      function frame(now) {
        var t = Math.min(1, (now - start) / dur);
        el.textContent = String(Math.round(target * easeOutCubic(t)));
        if (t < 1) requestAnimationFrame(frame);
        else el.textContent = String(target);
      }
      requestAnimationFrame(frame);
    });
  }
  var roots = document.querySelectorAll('[data-hero-count-root]');
  if (!roots.length) return;
  var reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  var io = new IntersectionObserver(function(entries){
    entries.forEach(function(e){
      if (!e.isIntersecting) return;
      var root = e.target;
      io.unobserve(root);
      if (reduced) setHeroCountFinals(root);
      else runHeroCountUp(root);
    });
  }, { threshold: 0.2, rootMargin: '0px 0px -8% 0px' });
  roots.forEach(function(el){ io.observe(el); });
})();
</script>
</body>
</html>
